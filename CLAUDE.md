read SPEC.md before making changes; all storage goes through repository.ts; do not add features not in the spec.

`TICKETS.md` in the repo root is the record of open work, decisions pending,
and deliberately parked items. Read it before proposing new work — if
something is already listed there as parked, don't raise it again. Never
start a ticket that hasn't been asked for in the current session.